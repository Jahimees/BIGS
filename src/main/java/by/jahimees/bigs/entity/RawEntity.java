package by.jahimees.bigs.entity;

/**
 * Interface which indicates that class which implements it has an entity objects.
 * These objects can be used with JPA.
 * <p>
 * New entity MUST implement this interface because services receive generic, which implements it.
 */
public interface RawEntity {
}
